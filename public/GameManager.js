class GameManager {
    constructor(scene, board, players, ui) {
        this.scene = scene;
        this.board = board;
        this.players = players;
        this.ui = ui;
    }

    update() {
        // Game logic update
    }
}

export default GameManager;